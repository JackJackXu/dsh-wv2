# dsh-wv2 — 验收与已知限制

> 记录"已实测通过"与"因缺少条件**未验证/受限**"的项，作为发布依据与后续跟进清单。
> 原则：**能自证的先自证；需要特殊环境（另一台机器/非宿主/长时间挂机/联网）的，如实标注，不空口说通过。**

## 一、已自证通过（当前环境实测）
- Release 构建：0 警告 / 0 错误；单文件发布成功
- NuGet 漏洞审计：`dotnet list package --vulnerable` 无已知漏洞
- 单元测试 **12/12**：SessionWatcher 3 个（基线不误报 / turn/end 只触发一次 / 审批字段读 data.*）+ DshUpdater 9 个（rc 版本语义 / 候选选择）
- 起服务 → token→cookie → WebView2 加载全链路可用（当前会话即运行其上）
- 任务完成 / 审批通知：会话日志事件读取实测通过
- WebView2 安全：导航锁、权限最小化、`IsWebMessageEnabled=false`、禁右键菜单、Profile 迁到 `%LOCALAPPDATA%`
- 崩溃自愈框架、加载失败退避重试、唤醒重载（静态/有限验证）

## 二、v1.1 新增功能 —— 编译通过、逻辑/单测已验，**运行待装新版本实测**（如实标注）
> 本会话即宿主进程，GUI/自动重启/升级流程无法在本机不中断地完整自测；以下均为代码级完成 + 必要的纯逻辑单测覆盖，需你用安装版实测一轮。

| v1.1 项 | 状态 |
|---|---|
| 冷启动并行化（WebView2 与 dsh 同起） | 逻辑已改；真实提速秒数待装机实测 |
| 托盘"有状态就闪"（统一闪法） | 逻辑完成；闪烁观感需真机看 |
| 通知气泡点击 → 显示主窗口 | 逻辑完成；真机点测 |
| 应用内日志查看器（分级/刷新/清空/开目录） | 逻辑完成；UI 真机看 |
| 托盘"检查 dsh 更新"（查+确认后一键 npm 升级） | 版本比较已单测；**联网抓取/升级需真实网络实测** |
| 健康检查：端口失联×3 或服务丢失 → 自动重启（防重入） | 逻辑完成；自动重启需非宿主环境验证（本机会踢掉会话） |
| PID 锁文件 + 陈旧锁清理 | 逻辑完成；异常退出场景需真机测 |
| 依赖自检（本地 dsh 版本读取/过旧警示） | 版本读取逻辑完成；路径各安装形态真机测 |
| 退出清理 WebView2 cookie（含 dsh-auth） | 逻辑完成；重启后登录态真机测 |
| 旧 WebView2 Profile 一键迁移 | 逻辑完成；需用旧版残留数据真机测迁移 |
| SessionWatcher 增量监听（FS watcher + 15s 兜底） | 轮询→增量改造；长时间/多会话 IO 需挂机观察 |

## 三、因缺少条件 → 未验证 / 受限（延续）
| 项 | 缺什么条件 | 影响/说明 |
|---|---|---|
| **破坏性实测**：杀 WebView2 渲染进程看自愈；连续重启 dsh ×5；退出后无孤儿 node/WebView2 | 需非宿主进程/另一机器 | 本实例即宿主，不能自杀测试 |
| **健康自动重启不误触发** | 需独立进程挂机 | 新增自动重启需确认不会在正常场景误重启 |
| **高 DPI**：150%/200%、混合 DPI | 需带高 DPI 显示器 | 清单已加 PerMonitorV2，真机未验 |
| **睡眠/唤醒**后自愈 | 需可睡眠实机 | 代码有 `PowerModeChanged` 重载，未实机验证 |
| **24h 挂机内存/句柄稳定性** | 需长时运行 | 仅短时观察无增长 |
| **Windows 10 / 其它版本** | 需 Win10 实机/虚拟机 | 仅在 Win11 24H2 实测 |
| **数字签名 / SmartScreen** | 需证书 + 分发决策 | 公开分发大概率触发 SmartScreen |
| **WebView2 历史含 token URL** | SDK 无"仅清历史"枚举；清 All 会登出 | 退出已清 cookie（含 dsh-auth）；token URL 仍会留在 History（已知残留） |
| **dsh 更新联网/升级** | 需真实网络 + npm 全局写权限 | npmmirror 可达性、`npm i -g`、npm11 allow-scripts 需实测 |

## 四、结论
- 上述"未验证项"**已如实记录为已知限制**，不因缺少环境而假装通过。
- 是否把 v1.1 装到你的机器上用：由你在看完本表后决定。建议：先装新版本跑一遍第二节的"真机点测"项（闪、气泡、日志器、迁移、退出后 cookie 清空），健康自动重启与 dsh 升级这两项放到非当前会话/你有心理准备再测。
