# dsh-wv2 — 验收与已知限制

> 记录"已实测通过"与"因缺少条件**未验证/受限**"的项，作为发布依据与后续跟进清单。
> 原则：**能自证的先自证；需要特殊环境（另一台机器/非宿主/长时间挂机）的，如实标注，不空口说通过。**

## 一、已自证通过（当前环境实测）
- Release 构建：0 警告 / 0 错误；单文件发布成功（主 exe ~2.5MB）
- NuGet 漏洞审计：`dotnet list package --vulnerable` 无已知漏洞
- 冷启动 ≈2.7s（单样本）、空闲 CPU <1%
- 起服务 → token→cookie → WebView2 加载全链路可用（当前会话即运行其上）
- 任务完成通知：用合成 zstd 会话日志实测通过（基线不误报、turn/end 触发、半帧不误报、子代理过滤）
- 崩溃自愈框架、加载失败退避重试、全局快捷键注册、唤醒重载（静态/有限验证）
- WebView2 安全：导航锁、权限最小化、`IsWebMessageEnabled=false`、Profile 迁到 `%LOCALAPPDATA%`

## 二、因缺少条件 → 未验证 / 受限（如实标注，非空口通过）

| 项 | 缺什么条件 | 影响/说明 |
|---|---|---|
| **自动化测试**（SessionWatcher 帧扫描/字段、DshProcess） | 未建测试工程 | 质量风险：曾有审批字段 bug 靠人眼/真机才暴露。**建议优先补**（不依赖特殊环境） |
| **破坏性实测**：杀 WebView2 渲染进程看自愈 | 需非宿主进程/另一机器 | 本实例即宿主，不能自杀测试 |
| **高 DPI**：150%/200%、混合 DPI | 需带高 DPI 显示器 | 清单已加 PerMonitorV2，真机未验 |
| **睡眠/唤醒**后自愈 | 需可睡眠的实机 | 代码有 `PowerModeChanged` 重载，未实机验证 |
| **连续重启 dsh ×5**、退出后无孤儿进程 | 需独立进程 | 代码加了代次 token / Dispose，未破坏性实测 |
| **24h 挂机内存/句柄稳定性** | 需长时运行 | 仅观察 ~20 分钟无增长 |
| **Windows 10 / 其它版本** | 需 Win10 实机/虚拟机 | 仅在 Win11 24H2 实测 |
| **数字签名 / SmartScreen** | 需证书 + 分发决策 | 公开分发大概率触发 SmartScreen，README 已注明 |
| **快捷键冲突/自定义**、**BUILD.md/publish.ps1** | 属功能/流程打磨 | 已延后，非发布硬阻塞 |

## 三、破坏性实测清单（给"有条件的环境"执行，通过后再宣告更广分发）
1. 用**非当前会话宿主**的另一进程/机器运行：
   - 杀 WebView2 渲染进程 → 应自动重载
   - 切 150% / 200% DPI（及混合 DPI）→ 界面不模糊、不错位
   - 睡眠 → 唤醒 → 服务/页面自动恢复
   - 连续点"重启服务" 5 次 → 每次都能起来、不永久卡"正在重启"
   - 退出后：任务管理器里 `msedgewebview2.exe` 与本次 node dsh **清零**（无孤儿）
2. 挂机 24h：记录内存/句柄曲线，确认无单调增长。

## 四、结论
- 上述"未验证项"**已如实记录为已知限制**，不因缺少环境而假装通过。
- 是否可发布：**由你在看完本表后决定**。若按"自用 + 接受这些限制"则可发（建议仍先补自动化测试）；若要面向更广分发/长期负责，则在有条件时完成破坏性实测 + 签名后再正式发。
