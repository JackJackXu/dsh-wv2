# Changelog — dsh-wv2

## [1.3.2] — 2026-09-11

**修掉「检查 dsh 更新」的 EBUSY（退出码 -4082）**。

### 修复
- 真因：npm 升级时要把**正在运行的** `@deepseek-ai\dsh\lib` 改名挪走，而 dsh 进程锁着该目录 → `EBUSY: resource busy or locked`（npm errno **-4082**），升级失败。
- 现在升级流程改为：**先停 dsh 服务（释放文件锁）→ 跑 npm → 无论成败都重新拉起服务**；期间压住健康检查的自动重启与"服务意外退出"提示。
- 失败时若检测到 `EBUSY`/`-4082`，额外提示"确认没有其它 dsh 进程在运行"。
- 确认框也改为明说"升级会先停止 dsh 服务（页面短暂断开，完成后自动重启）"。

## [1.3.1] — 2026-09-11

**加固「检查 dsh 更新」**，让失败可诊断、不再只报一个退出码。

### 修复 / 改进
- 升级走**专用 npm 缓存** `%LOCALAPPDATA%\DSH WV2\npm-cache`，避开共享 `%LOCALAPPDATA%\npm-cache` 被占用/杀软拦截导致的 EPERM。
- npm 的**完整输出 + 退出码写入日志**（`error.log`，可用托盘"日志查看器"看）。
- 升级失败弹窗改为显示 **npm 输出的最后若干行**，而不只是"退出码 X"。
- 加 `--no-audit --no-fund` 降低无关输出。

## [1.3.0] — 2026-09-08

**适配 dsh 0.1.5 的会话格式 V3**（为升级到 `0.1.5-rc.1` 做准备）。

### 兼容
- dsh 0.1.5 起会话日志改为**代次命名**：`session.jsonl.zstd`(v0) → `session.v3.jsonl.zstd`。
  SessionWatcher 由"精确匹配 `session.jsonl.zstd`"放宽为支持 `session*.jsonl.zstd`，
  使升级后**任务/审批/提问通知继续可用**（V3 的表头与事件行格式均未变，已用官方样例核对）。
- 同一会话目录可能同时存在"旧代冻结 + 新代活跃"多个日志；现按**代次号取最高**的那个，
  避免读取冻结文件导致漏报/重复通知。
- 单测 +2（V3 文件名可被监听、多代次取最高），共 **16/16**。

## [1.2.0] — 2026-09-05

**提问通知做出来了**——翻 dsh 源码后发现，`ask_user_question` 是普通 tool，其 `tool/call`（带完整 `arguments` 问题 JSON）在**提问那一刻**就落盘到会话日志（`tool/result` 才是回答后）。因此不需要 DOM 桥、也不需要等上游加事件，直接从会话日志就能得知"dsh 正在问你问题"。

### 新增
- **提问通知**：SessionWatcher 识别 `tool/call` 的 `ask_user_question`，解析问题/选项数生成摘要，弹托盘气泡 + 经典闪烁（受"通知"开关与聚焦逻辑约束，同审批）；点击气泡把窗口拉到前台
- 单测新增 2 个（提问只触发一次、非提问工具不误报），共 **14/14**
- 版本升至 **1.2.0**

## [1.1.0] — 2026-09-05

v1.1 增强：补齐"高人审计"中改动小、价值高的项（除提问通知——仍需 dsh 上游提供落盘事件，见下）。

### UX / 托盘
- **托盘"有状态就闪"**：任务完成 / 需要审批 / dsh 异常时，托盘图标用经典"出现→消失→出现"循环闪烁（不区分类型），**常开**且**持续闪到窗口被聚焦**。"通知"开关只控制**气泡**：开 → 先弹气泡、等它消失后再接续闪（避免把气泡顶掉）；关 → 直接开始闪、不弹气泡
- **通知气泡可点击**：点任务/审批气泡 → 强拉到前台显示主窗口（含 SetForegroundWindow 延时重试，解决后台程序抢不到焦点）；恢复时**保留原最大化/窗口状态**（不再被 SW_RESTORE 还原成窗口模式）
- **应用内日志查看器**：托盘新增"日志查看器"，Info/Warn/Error 分级过滤、自动刷新、打开目录、清空（日志写入加了 `[INFO]/[WARN]/[ERROR]` 级别前缀）
- **托盘新增"检查 dsh 更新"**：读本地 dsh 版本 → 联网查 npmmirror（latest/next）→ 发现新版先弹提示，你确认后才 `npm i -g` 一键升级，完成后提示重启 dsh 服务生效
- 删除全局 **Ctrl+Alt+D 热键**（按你的要求整个移除）

### 稳定性 / 安全
- **健康检查**：不再只靠"进程是否存活"——周期探测 dsh 端口；服务意外退出或进程活着但端口失联连续 3 次 → 自动重启（带防重入保护）
- **PID 锁文件**：启动记录 PID，发现上个进程已死留下的陈旧锁会清理（端口冲突仍由 dsh 自动回退 port 0 兜底）
- **依赖自检**：启动记录并校验本地 dsh 版本，0.0.x 等过旧代际给出日志警示
- **退出清理 cookie**：退出时清空 WebView2 会话 cookie（含 dsh-auth），下次启动自动用新 token 重新鉴权
- **安全纵深**：额外禁用页面右键菜单（`AreDefaultContextMenusEnabled=false`）
- **旧 WebView2 Profile 一键迁移**：检测到 1.0 及更早时代"程序旁边"的旧数据时，询问并迁移到 `%LOCALAPPDATA%\DSH WV2\WebView2`

### 性能
- **冷启动并行化**：WebView2 初始化与 dsh 服务启动并行进行（原串行等待），URL 就绪后自动接续导航

### 工程
- `SessionWatcher` 由"每 2 秒全目录轮询"升级为 **文件系统增量监听 + 15 秒低频兜底**，降低空闲 IO
- 单测新增 `DshUpdaterTests`（版本比较 rc 语义、候选选择），共 12 个全部通过
- 提问通知仍未实现：需 dsh 上游提供落盘 `question/asked` 事件或宿主 API（不采用脆弱的 DOM 桥）

## [1.0.0] — 2026-09-05

首个正式版。C#/WPF + WebView2 的 DeepSeek Harness 桌面壳（替代 Electron 版 dsh-lite，不打包 Chromium）。

### 核心
- WebView2 打开 dsh WebUI；复用系统 node + 全局 dsh（共享 `~/.dsh`）
- 托盘驻留：完整菜单（打开/重载/重启服务/数据目录/日志目录/终端/通知/开机自启/关于/退出）
- 单实例 + 错误捕获 + 崩溃/加载自愈 + Ctrl+Alt+D 全局快捷键 + 唤醒重载
- 窗口大小/位置记忆（越界校验）、开机自启（注册表 Run）
- 任务完成通知（会话日志 zstd 轮询）；审批通知（会话日志 `approval/asked`）
- 安全：导航锁、权限最小化、外链走系统浏览器、无原生桥

### 说明
- 提问（自由问答）通知未实现：dsh 走宿主↔UI 内部 RPC，不落盘、无外部干净通道。
- 曾尝试 mux WebSocket / DOM 观察两条审批通知路线，后因 dsh 0.1.2 无干净接口而废弃，改用会话日志 `approval/asked`。

### 发布硬化（RC2 复审后）
- 审批通知读 `approval/asked` 的 `data.*` 字段；端口被占用自动回退 `--port 0`
- 服务进程用 **Job Object** 绑定，宿主崩溃/强杀即回收 node 树；重启用代次 token 过滤旧事件
- WebView2 Profile 迁到 `%LOCALAPPDATA%\DSH WV2\WebView2`；关 `IsWebMessageEnabled`/自动填充/密码保存
- 单实例第二次启动唤醒已有窗口；高 DPI（PerMonitorV2）清单
- 加载动画、成功后再揭遮罩、失败可操作（重试/日志/终端）、首关托盘提示、About 补 MIT
- 日志 5MB 轮转；`dotnet test` 3 个 SessionWatcher 单元测试纳入 CI

### 工程
- .NET 8，框架依赖单文件发布（~2.5MB），CI build+test+publish。
